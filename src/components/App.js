import React, { Component } from 'react';

import Section from './Section/Section';

class App extends Component {
  state = {
  };

  render() {
    const {  } = this.state;
    return (
      <>
        <Section title="">
        </Section>
      </>
    );
  }
}

export default App;
